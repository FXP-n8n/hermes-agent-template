starlette>=0.40.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
# websockets is the upstream client our reverse proxy uses to connect to
# hermes's /api/pty, /api/ws, /api/events endpoints. httpx does not speak
# WebSockets, so we need a dedicated client library.
websockets>=12.0

# ChromaDB + embeddings — used by knowledge extractor, briefings, and fleet audit
chromadb>=0.5.0
sentence-transformers>=3.0.0
# trigger-rebuild-202605310546
